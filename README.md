# xSMT

An implementation of 'Sparse Merkle Tree' with versioned features, ported from [sparse_merkle_tree](https://crates.io/crates/sparse-merkle-tree).

## Examples

```rust
type Smt = xsmt::VsSmt<Vec<u8>>;

let smt = Smt::default();
...

```

[**SEE ALSO**](./docs/README_origin.md)
